{
  "name": "@guardrails/core",
  "version": "0.1.0",
  "description": "Orchestration layer: composes detection, policy, and redaction, with education and filesystem scanning.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -b tsconfig.build.json",
    "typecheck": "tsc -b tsconfig.build.json"
  },
  "dependencies": {
    "@guardrails/policy-engine": "workspace:*",
    "@guardrails/redaction": "workspace:*",
    "@guardrails/secret-detector": "workspace:*",
    "@guardrails/shared": "workspace:*"
  }
}
